### npm install 이용해서 한번에 설치
npm install @react-navigation/drawer
npm install react-native-gesturehandler react-native-reanimated

### reset babel plugin and reset cache
expo r -c

### WARNING: The legacy expo-cli does not support Node +17. Migrate to the versioned Expo CLI (npx expo).
npm i -g expo-cli
npx expo-cli upgrade
npx expo install react-native@0.71.4
npx expo start